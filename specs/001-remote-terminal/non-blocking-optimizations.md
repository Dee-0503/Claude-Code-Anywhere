# Non-blocking Optimization Backlog: Remote Terminal

用于阶段完成后整体评估，不阻塞当前 US7 PR 合并。

| ID      | Area                        | Suggestion                                                                                                     | Why                                                                                       | Suggested evaluation point                                           |
| ------- | --------------------------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| NBO-001 | Terminal scaling            | Replace hardcoded `characterWidth: 8` in `TerminalView` with measured or configurable xterm character width.   | Different fonts, zoom levels, and platforms may make fixed width inaccurate.              | After US7/US8 completion, before final polish/performance pass.      |
| NBO-002 | Terminal output rendering   | Replace 1MB fallback text string concat/slice with chunked or ring-buffer-backed text storage.                 | High-frequency output may make repeated O(n) string operations visible on slower devices. | During final performance validation with large-output scenarios.     |
| NBO-003 | Input recovery offsets      | Initialize the frontend input client from the persisted `lastInputOffset + 1` instead of always starting at 1. | Long-lived sessions that reconnect may otherwise risk treating fresh input as old input.  | Before or during the next reconnect/input recovery hardening pass.   |
| NBO-004 | Fallback input queuing      | Let fallback terminal input use the input client queue while offline instead of requiring an open socket.      | Offline fallback input can otherwise be dropped while primary input is already queued.    | Before or during the next weak-network UX validation pass.           |
| NBO-005 | Input ACK recovery tracking | Advance persisted input recovery state from monotonic server ACK offsets, with tests for out-of-order ACKs.    | Counting ACKs locally can drift if ACKs are reordered or replayed after reconnect.        | Before or during the next input protocol resilience validation pass. |
